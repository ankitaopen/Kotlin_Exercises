//type conversion in kotlin using helper function
fun main(args : Array<String>)
{
    var value1 : Int=1000;
    var value2 : Long = value1.toLong()
    println(value2)
    //here we are using var----this is because var is mutable variable
    //type conversion in kotlin using helper function
    //converting Int to Long
    //conversion of larger datatype to smaller datatype
}