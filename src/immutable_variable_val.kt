//val
//val is an immutable variable
//we cannot change the value of the variable which is declared using val keyword
fun main(args : Array<String>)
{
    val variable:Int=100
    println("the value of val variable is:" +variable);
    //here we cannot re-assing this val variable as its immutable
    //val variable=200
    //println("the value of val variable is:" +variable)
}