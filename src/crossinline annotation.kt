//crossinline annotation
//to prevent return form lambda expression and inline function itself we can mark the lambda expression as crossinline
//this will throw a compiler error if found a return statement inside that lambda expression
